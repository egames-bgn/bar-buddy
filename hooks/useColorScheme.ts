/**
 * Simplified color scheme hook that always returns 'light'
 * This ensures consistent appearance across the app
 */

export function useColorScheme(): 'light' {
  // Always return light mode for consistent appearance
  return 'light';
}
