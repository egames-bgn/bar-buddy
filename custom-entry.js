// This is a custom entry point for bundling the app
// First import the window polyfill to ensure window.addEventListener is available
import './window-polyfill';

// Then import the Expo Router entry point which will load your app
import 'expo-router/entry';
